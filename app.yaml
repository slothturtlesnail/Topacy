name: topacy-static
static_sites:
- name: topacy-static
  github:
    branch: master
    deploy_on_push: true
    repo: slothturtlesnail/topacy
  source_dir: public
  build_command: echo "No build required for static site"
  output_dir: public
  routes:
  - path: / 